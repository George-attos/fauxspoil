import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'FauxSpoil - Your AI Spoiler Dealer',
  description: 'Absurdly believable spoilers for shows, movies & unreleased chaos',
  keywords: 'spoilers, fake spoilers, AI, entertainment, movies, TV shows',
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
